# Copy to .env and fill in. .env is gitignored — never commit it.

# --- Choose ONE path and keep it for the whole 10 weeks ---
# Switching paths mid-course invalidates every baseline you have recorded.

# Path A — hosted provider (OpenAI-compatible)
MODEL_BASE_URL=https://api.openai.com/v1
MODEL_API_KEY=sk-replace-me
MODEL_NAME=gpt-4o-mini

# Path B — local, no cost (Ollama). Uncomment these and comment out Path A.
# MODEL_BASE_URL=http://localhost:11434/v1
# MODEL_API_KEY=ollama
# MODEL_NAME=llama3.2
